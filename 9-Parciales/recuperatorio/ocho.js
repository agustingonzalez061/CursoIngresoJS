function Mostrar()
{
    var numero
    var contador = 0;
    var contadorPar = 0;
    var acumulador = 0;
    var promedio
    var numeroMax
    var numeroMin
    var respuesta = "Si"
    while (respuesta == "Si")
    {
        numero = prompt ("Ingrese numeros positivos")
        if (numero = null)
        {
            break;
        }
        while (numero <0)
        {
            numero = prompt ("Los numeros deben ser positivos")
        }
    }
}
